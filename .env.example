# Google Generative AI API key
# Obtain from Google AI Studio: https://aistudio.google.com/
GOOGLE_API_KEY=your_api_key_here

# Optional: override the default model used by the server
# Examples: gemini-2.5-flash-image-preview (default), gemini-1.5-pro
GEMINI_MODEL=gemini-2.5-flash-image-preview

# Optional: server port (default 3000)
PORT=3000

# Optional: CORS allowed origins (comma-separated)
# Example: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=

# Optional: rate limit maximum requests per 15 minutes per IP (default 20)
RATE_LIMIT_MAX=20

# Optional: Gemini request timeout in milliseconds (default 30000)
GEMINI_TIMEOUT_MS=30000
